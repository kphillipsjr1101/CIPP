interface:
  display_name: "CIPP"
  short_description: "Repo-specific patterns and workflows for CIPP"
  default_prompt: "Use the CIPP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true